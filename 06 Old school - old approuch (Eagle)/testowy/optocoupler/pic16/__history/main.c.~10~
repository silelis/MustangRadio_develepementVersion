#include <main.h>

void main()
{

   while(TRUE)
   {

      //Example blinking LED program
      output_low(LED);
      delay_ms(DELAY);
      output_high(LED);
      delay_ms(DELAY);

      //TODO: User Code
      printf("D");
      delay_ms(10);
      printf("a");
      delay_ms(10);
      printf("w");
      delay_ms(10);
      printf("i");
      delay_ms(10);
      printf("d");
      delay_ms(10);
   }

}
