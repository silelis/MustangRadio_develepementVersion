#include <ostream>
#include <stdio.h>
#include <stdbool.h>
#include <unistd.h>

#include <iostream>

using std::cout;
using std::endl;
using std::runtime_error;

//void app_main(void)
extern "C" void app_main(void)
{
    while (true) {
        //printf("Hello from app_main!\n");
        cout<<"TATA"<<endl;
        sleep(1);
        btstack_init();
    }
}
